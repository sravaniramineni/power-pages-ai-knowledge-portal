# ADR-001: Confidence-gated answers with caching

## Context
Portal users lose trust when AI answers are wrong but confident.

## Decision
Every answer carries a confidence score and citations; below threshold the API
suggests opening a ticket with context; frequent queries are TTL-cached.

## Consequences
- Fewer hallucinations reach users.
- Cache must respect article publish state and permissions.
